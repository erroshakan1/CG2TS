# CG2TS v4 Requirements
# NOTE: numpy version is important for reproducibility and compatibility with
# numerical and scientific packages. The version below is the tested one.

numpy==1.24.4
MDAnalysis>=2.0.0
matplotlib>=3.5.0
scikit-learn>=1.0.0
scipy>=1.9.0
open3d>=0.15.0

# Optional but recommended for nicer output and progress
tqdm>=4.60.0
seaborn>=0.11.0

# Development / interactive
ipython>=7.0.0
pytest>=7.0.0
